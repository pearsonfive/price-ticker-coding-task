<Window x:Class="PriceTicker.HistoryScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceTicker"
        mc:Ignorable="d"
        Title="HistoryScreen" Height="450" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" MinHeight="20" Background="CornflowerBlue" VerticalAlignment="Center">
            <Label Content="{Binding Name}" Foreground="White" VerticalContentAlignment="Center" FontSize="16" FontWeight="DemiBold"/>
        </StackPanel>
        <ListView ItemsSource="{Binding History}" Grid.Row="1" Margin="2" BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date/Time" DisplayMemberBinding="{Binding DateTimeString}"/>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
